{
  "name": "tryon-rvw",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/run-dev.mjs",
    "dev:stop": "node scripts/free-dev-ports.mjs",
    "build": "npm ci --prefix frontend && npm run build --prefix frontend",
    "pages:build": "npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
